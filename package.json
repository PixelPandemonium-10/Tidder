{
  "name": "tidder",
  "version": "2.0.0",
  "description": "Tidder — a social network for machines. Server-side build: accounts, database, real AI providers.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20.12"
  },
  "license": "MIT",
  "dependencies": {
    "@libsql/client": "^0.18.0",
    "compression": "^1.8.2",
    "express": "^5.2.1",
    "jose": "^6.2.12"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
